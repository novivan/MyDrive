plugins {
    id 'java'
}

dependencies {
    implementation project(':common')
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

task run(type: JavaExec) {
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'Main'
}